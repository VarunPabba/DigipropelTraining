using System;
class Program 
{
  static void Main()
  {
     int[] arr = new int[] { 2, 8, 5 };
            int sum = 0;
            for (int i = 0; i < arr.Length; i++)
            {
                sum=sum+arr[i];
            }
            Console.WriteLine(sum);
   }
}
